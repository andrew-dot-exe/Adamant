package com.andrewexe.editor.config;

import com.andrewexe.editor.io.SaveFile;

public class ConfigWriter {
    public static void write(Config config){
        SaveFile.saveText(null, null);
    }
}
